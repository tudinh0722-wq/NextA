# NextA — Claude Implementation Contract

## 1. Mission

Implement and maintain the **Flutter v2 product** on branch `flutter-v2`.

Repository: `tudinh0722-wq/NextA`

The Flutter implementation is the product being developed. The legacy Android/Kotlin implementation exists only as **reference material** for concepts, behavior, historical decisions, and platform ideas.

## 2. Branch and source-of-truth rules

### MUST
- Work on `flutter-v2`.
- Treat `flutter/` as the active product source tree.
- Read `AGENTS.md` and the relevant `docs/*.md` files before changing architecture or behavior.
- Preserve working behavior unless the task explicitly changes it.
- Keep event/countdown semantics independent from presentation surfaces.
- Use Material 3 semantic/dynamic colors where the design does not explicitly require the fixed event-priority palette.
- Keep the shared event-priority palette consistent across Editor, Agenda, and Calendar.
- Update `docs/task.md`, `docs/changelog.md`, and `docs/plan.md` after meaningful product changes; update `docs/architecture.md` when architecture changes.

### MUST NOT
- Do **not** implement Flutter features by modifying `app/`.
- Do **not** treat Kotlin/Compose code as the current implementation.
- Do **not** copy legacy Android architecture into Flutter merely because it exists there.
- Do **not** replace SQLite with Room for Flutter.
- Do **not** introduce a separate recurrence-rule table unless explicitly requested.
- Do **not** introduce Jetpack Glance for the Home Widget unless explicitly requested.
- Do **not** assume an Android AppWidget is universally available on Lock Screen.
- Do **not** use device-brand checks where capability-oriented checks are possible.
- Do **not** add per-second/per-event countdown timers.
- Do **not** claim a build, test, or device verification that was not actually performed.

## 3. Current implementation

### Flutter planner
The current planner already has:
- month-first calendar hierarchy;
- month/week collapse and expansion;
- directional month navigation;
- week navigation by whole weeks;
- selected-day agenda;
- event create/edit/delete/search;
- SQLite persistence;
- Material 3 theming;
- minute-aligned countdown refresh;
- countdown formatting with hour precision for multi-day durations;
- Samsung Calendar-inspired Add Event editor;
- priority selection and persistence;
- reminder configuration and repeat metadata;
- finite recurrence generation and recurrence UI;
- calendar quick tap and long press behavior;
- non-interactive empty agenda state;
- AI Import placeholder reserved for the bulk-import workflow.

### Event data
`NextAEvent` is a concrete dated occurrence.

Recurring occurrences:
- share `recurrenceId`;
- carry recurrence metadata on concrete occurrences;
- are generated by the pure recurrence policy before persistence;
- are finite by count or end date, with a hard safety cap.

SQLite (`nexta.db`) is the Flutter source of truth.

### Priority
The shared event priority palette is:
- normal: pastel blue;
- important: pastel orange;
- very important: pastel red.

The same persisted priority must drive:
- event editor priority dot;
- agenda countdown outline;
- calendar event markers/background tint.

## 4. Current UI contract

### Add Event
Current hierarchy:
1. compact `Thêm sự kiện | AI Import` header;
2. title + compact priority dot;
3. start/end date-time columns;
4. `Địa chỉ`;
5. `Ghi chú`;
6. reminder configuration;
7. reminder repeat;
8. recurrence;
9. floating `Thoát | Lưu` action pill.

Rules:
- title max 47 characters;
- address max 30 characters;
- note max 30 characters;
- no All-day control;
- no separate priority row;
- priority selector expands horizontally at the title dot;
- date/time text is tappable without calendar/clock icons;
- recurrence uses a centered radio dialog;
- recurring frequency expands an indented finite end section;
- default recurrence count is 10;
- reminder default is 10 minutes before;
- default repeat is 2 additional reminders at 5-minute intervals;
- disabled reminder persists as `reminderMinutes = 0`.

### Calendar markers
Calendar event markers are limited to two bars per day.

**Required visual rule:** bars are stacked **vertically**, not diagonally/horizontally inset. Every marker bar must have the same width. Priority only changes the bar color. Do not make the second bar shorter by increasing left/right inset.

### Agenda event time block
The start/end time and countdown belong to one standardized right-side time block.

Required visual rule:
- start/end time is visually prominent;
- start/end text is centered;
- countdown surface uses the same width as the start/end time block;
- countdown label and value are centered;
- spacing and typography should come from shared constants/components rather than scattered magic numbers.

### Editor icon alignment
`Địa chỉ` and `Ghi chú` icons must align with the other editor leading icons. Avoid the default `TextField.prefixIcon` 48px inset if it causes visual misalignment. Use an explicit shared leading-icon slot/constraint.

## 5. UI refactoring policy

When touching planner UI, prefer a small design-system layer over repeated ad-hoc values.

Centralize where practical:
- event priority colors;
- planner/editor horizontal gutters;
- event time column width;
- icon slot width;
- recurring/reminder row heights;
- common corner radii;
- common typography roles;
- countdown surface dimensions.

Remove dead widgets, unused callbacks, duplicated calculations, obsolete comments, and imports made unnecessary by refactoring.

Do not refactor business logic merely for style while changing a UI task. Separate structural cleanup from behavior changes and preserve behavior.

## 6. Platform surfaces

NextA has separate presentation responsibilities:

1. **App Screen** — detailed planner/navigation.
2. **Home Widget** — glanceable Today Schedule using Android `RemoteViews`.
3. **Focus/Lock Screen** — Now & Next with large countdown where the host platform permits it.
4. **Notification fallback** — used where a dedicated Lock Screen surface is unavailable.

These surfaces share semantics but should not be forced into one UI implementation.

## 7. What remains

Highest-priority work:
1. Finish UI normalization/refactoring without regressing the current planner.
2. Verify Calendar marker vertical stacking and Agenda time/countdown sizing on a real device.
3. Finish series-aware recurrence edit/delete behavior if not already complete in the current branch.
4. Replace the `AI Import` placeholder with AI-assisted bulk import, including preview and validation.
5. Add bulk-import integration tests.
6. Re-verify Home Widget and Focus/Lock Screen after data-model integration.
7. Perform broader responsive/cross-device verification.

## 8. Verification discipline

Before declaring work complete:
- inspect the actual current `flutter-v2` files rather than relying only on this document;
- run the relevant Flutter formatter/analyzer/tests when available;
- inspect the diff for accidental changes outside scope;
- report exactly what was verified and what was not.

## 9. Interpretation of the repository

Think of the repository as:

```text
flutter/                 = CURRENT PRODUCT — IMPLEMENT HERE
├── domain/              = shared event semantics
├── application/         = recurrence/countdown policies
├── data/                = SQLite persistence
└── presentation/        = Flutter planner/editor UI

app/                     = LEGACY REFERENCE — DO NOT MODIFY FOR FLUTTER WORK

 docs/                   = project contracts, progress, architecture, decisions
```

If a legacy Android file conflicts with the Flutter implementation or this contract, **Flutter v2 + current docs win**.

If the requested task is ambiguous, first inspect the current Flutter implementation and docs, then choose the smallest change that preserves the established architecture and UX.
